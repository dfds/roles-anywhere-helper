project_name: roles-anywhere-helper
builds:
  - env: [ ]
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    id: "roles-anywhere-helper"
    dir: ./src
    main: ./

release:
  draft: true

changelog:
  skip: false

  use: git
  sort: asc
